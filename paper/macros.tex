% Custom commands: product names, acronyms, and the results placeholder.

% --- Product names ---
% \populace / \populacefit render the current product names (Microcosm,
% microcosm-fit); the macro names keep the legacy spelling so the rename
% stays a two-line change here rather than a source-wide sweep.
\newcommand{\policyengine}{\textsc{PolicyEngine}}
\newcommand{\populace}{\textsc{Microcosm}}
\newcommand{\populacefit}{\texttt{microcosm-fit}}
\newcommand{\microimpute}{\texttt{microimpute}}
\newcommand{\pystatmatch}{\texttt{py-statmatch}}

% --- Survey and data acronyms ---
\newcommand{\cps}{\textsc{CPS}}
\newcommand{\scf}{\textsc{SCF}}
\newcommand{\sipp}{\textsc{SIPP}}
\newcommand{\psid}{\textsc{PSID}}
\newcommand{\puf}{\textsc{PUF}}

% --- Method acronyms ---
\newcommand{\qrfacro}{\textsc{QRF}}
\newcommand{\prdc}{\textsc{PRDC}}

% --- Results placeholder ---
% Every empirical number enters the manuscript through a generated table or an
% explicit replacement of one of these. It renders loudly so a draft cannot
% ship with a silent gap, and grep-ably ("TODO") so none survives to submission.
\newcommand{\todo}[1]{\textbf{[TODO: #1]}}
