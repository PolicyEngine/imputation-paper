% Custom commands: product names, acronyms, and the results placeholder.

% --- Product names ---
\newcommand{\policyengine}{PolicyEngine}
\newcommand{\populace}{\textsc{Microcosm}}
\newcommand{\populacefit}{\textsc{Microcosm Fit}}
\newcommand{\chronicle}{\textsc{Chronicle}}
\newcommand{\microimpute}{\textsc{Microimpute}}
\newcommand{\pystatmatch}{\texttt{py-statmatch}}

% --- Survey and data acronyms ---
\newcommand{\cps}{\textsc{CPS}}
\newcommand{\scf}{\textsc{SCF}}
\newcommand{\sipp}{\textsc{SIPP}}
\newcommand{\psid}{\textsc{PSID}}
\newcommand{\puf}{\textsc{PUF}}

% --- Method acronyms ---
\newcommand{\qrfacro}{\textsc{QRF}}
\newcommand{\prdc}{\textsc{PRDC}}

% --- Results placeholder ---
% Every empirical number enters the manuscript through a generated table or an
% explicit replacement of one of these. It renders loudly so a draft cannot
% ship with a silent gap, and grep-ably ("TODO") so none survives to submission.
\newcommand{\todo}[1]{\textbf{[TODO: #1]}}

% --- From the sparsity paper (deduplicated) ---
% Custom commands and mathematics macros

% --- Acronyms and product names ---
\newcommand{\microcalibrate}{\textsc{Microcalibrate}}
\newcommand{\acs}{\textsc{ACS}}

% --- L0 / Hard Concrete notation ---
\newcommand{\lzero}{$L_0$}
\newcommand{\ltwo}{$L_2$}
\newcommand{\hc}{Hard Concrete}

% --- Placeholder for empirical results ---
\newcommand{\tbc}[1][]{%
  \textbf{[TBC%
    \def\temparg{#1}%
    \ifx\temparg\empty\else: #1\fi
  ]}%
}

% --- Math shortcuts ---
\newcommand{\E}{\mathbb{E}}
\DeclareMathOperator{\clip}{clip}
\DeclareMathOperator*{\argmin}{arg\,min}
\newcommand{\bw}{\mathbf{w}}
\newcommand{\balpha}{\boldsymbol{\alpha}}
\newcommand{\R}{\mathbb{R}}
