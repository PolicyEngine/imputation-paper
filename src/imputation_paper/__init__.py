"""Benchmarks for populace's weight-aware imputation approach.

This package holds the reproducibility code for the imputation paper: the
method-surface registry (:mod:`imputation_paper.methods`), the weighted
evaluation metrics (:mod:`imputation_paper.experiments.metrics`), a seeded toy
dataset generator (:mod:`imputation_paper.smoke`), and the ``imp`` command line
(:mod:`imputation_paper.cli`).
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
